# `orchestrator_agentv1.py`

- Orchestrator & Sub-agents (`ToolFinalOutput`)
    - Sub-agents results return to orchestrator (better results, slower, lesser tokens)

# `orchestrator_agentv2.py`

- Orchestrator only (with all tools)
    - Faster but costlier